# Fall Risk Forecasting - Incremental Mode
# Python Dependencies

# Core Data Processing
pandas>=1.3.0
numpy>=1.21.0
pyarrow>=6.0.0

# Machine Learning
scikit-learn>=1.0.0
xgboost>=1.5.0
imbalanced-learn>=0.9.0

# Visualization
matplotlib>=3.3.0
seaborn>=0.11.0

# Python 3.7+ required

# Install all dependencies:
# pip install -r requirements.txt

# Optional: For faster XGBoost training on GPU
# pip install xgboost[gpu]

# Notes:
# - pyarrow is required for Parquet file support (recommended for 5x faster I/O)
# - seaborn is optional (gracefully handled if missing)
# - imbalanced-learn is optional (SMOTE disabled by default)
